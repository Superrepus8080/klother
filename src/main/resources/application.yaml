app:
  port: 8080
  devMode: true
  measureServiceUrl: "http://localhost:9090"

  db:
    host: localhost
    port: 5432
    name: klother
    user: klother
    password: klother

  jwt:
    secret: "change-me-in-production-use-at-least-32-chars"
    expirySeconds: 86400   # 24 h
